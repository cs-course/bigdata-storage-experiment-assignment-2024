#include<stdio.h>

#include"config.h"
#include"functions.h"

int key[NUM], next[NUM];
int main()
{
    memset(next, -1, sizeof(next));
    memset(key, -1, sizeof(key));
    for(int i=0;i<n;i++)
    {
        
    }
}